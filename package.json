{
  "name": "back-end",
  "version": "1.0.0",
  "description": "",
  "main": "config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js",
    "build": "echo 'build start!' && npm install && npm run build-app && npm test && rm -rf dist && mkdir dist && cp -r public dist && cp -r server dist && cp package.json dist && cp package-lock.json dist && echo 'build complete!'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.3.3",
    "bcrypt": "^5.1.0",
    "colors": "^1.4.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "gp": "^0.0.1",
    "jsonschema": "^1.4.1",
    "jsonwebtoken": "^9.0.0",
    "jwt-decode": "^3.1.2",
    "morgan": "^1.10.0",
    "nodemon": "^2.0.12",
    "pg": "^8.9.0"
  }
}
